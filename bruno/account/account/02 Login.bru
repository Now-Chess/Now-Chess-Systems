meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{accountBaseUrl}}/account/login
  body: json
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "player1",
    "password": "securepassword"
  }
}

meta {
  name: Delete Bot
  type: http
  seq: 9
}

delete {
  url: {{accountBaseUrl}}/account/bots/{{botId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

headers {
  Authorization: Bearer {{token}}
}
